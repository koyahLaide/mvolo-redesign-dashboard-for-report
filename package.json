{
  "name": "mvolo-dashboard",
  "version": "1.0.0",
  "description": "Order attribution dashboard — syncs Shopify orders and attributes them to marketing channels",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "sync": "node -e \"require('dotenv').config(); require('./src/etl/sync').runSync()\"",
    "spend-sync": "node -e \"require('dotenv').config(); const {initDb}=require('./src/db/schema'); const {runSpendSync}=require('./src/etl/spend-sync'); runSpendSync(initDb())\"",
    "get-token": "node src/setup/get-token.js"
  },
  "dependencies": {
    "@google-analytics/data": "^5.2.1",
    "axios": "^1.6.7",
    "better-sqlite3": "^12.8.0",
    "chalk": "^4.1.2",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "googleapis": "^171.4.0",
    "node-cron": "^3.0.3",
    "open": "^8.4.2"
  }
}