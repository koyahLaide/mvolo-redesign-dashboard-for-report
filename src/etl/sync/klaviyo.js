const chalk = require('chalk');
const { initDb } = require('../db/schema');
db = initDb()

async function syncKlaviyo(db) {
    
    return ({length: orders.length,
            attributedOrders: attributedOrders,
            ordersNew: ordersNew
    })
}

module.exports = { syncKlaviyo }